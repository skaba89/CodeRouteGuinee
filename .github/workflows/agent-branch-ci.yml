name: Agent Branch CI

on:
  push:
    branches:
      - 'agent/**'
  workflow_dispatch:

jobs:
  backend-targeted:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install backend dependencies
        working-directory: backend
        run: pip install -q -r requirements.txt
      - name: Compile backend
        working-directory: backend
        run: python -m compileall -q app
      - name: Run targeted regression tests
        working-directory: backend
        env:
          PYTHONPATH: .
          DATABASE_URL: sqlite+pysqlite:///:memory:
          AUTO_CREATE_TABLES: 'true'
          ENVIRONMENT: test
        run: |
          candidates=(
            tests/test_exam_attempt_access.py
            tests/test_exam_runtime_recovery.py
            tests/test_media_policy.py
            tests/test_question_media_schema_compat.py
            tests/test_e2e_exam_question_trace.py
            tests/test_e2e_exam_certificate.py
            tests/test_e2e_candidate_center_multimedia_exam.py
            tests/test_question_governance.py
            tests/test_health.py
          )
          selected=()
          for file in "${candidates[@]}"; do
            if [ -f "$file" ]; then
              selected+=("$file")
            fi
          done
          if [ "${#selected[@]}" -eq 0 ]; then
            echo "No targeted backend tests found on this branch."
            exit 0
          fi
          printf 'Running targeted backend tests:\n - %s\n' "${selected[@]}"
          pytest -q --tb=short --disable-warnings "${selected[@]}"

  frontend-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci
      - name: Typecheck frontend
        working-directory: frontend
        run: npm run typecheck
      - name: Build frontend
        working-directory: frontend
        run: npm run build

  frontend-targeted-e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci
      - name: Run targeted Playwright regressions when present
        working-directory: frontend
        env:
          VITE_API_BASE_URL: http://localhost:8000
          CI: 'true'
        run: |
          candidates=(
            tests/e2e/exam-runtime-resilience.spec.ts
            tests/e2e/media-policy.spec.ts
          )
          selected=()
          for file in "${candidates[@]}"; do
            if [ -f "$file" ]; then
              selected+=("$file")
            fi
          done
          if [ "${#selected[@]}" -eq 0 ]; then
            echo "No targeted frontend E2E tests found on this branch."
            exit 0
          fi
          echo "Installing Chromium for targeted Playwright tests."
          npx playwright install --with-deps chromium
          printf 'Running targeted frontend tests:\n - %s\n' "${selected[@]}"
          npx playwright test "${selected[@]}" --timeout=30000 --retries=1

  publish-agent-statuses:
    if: always()
    runs-on: ubuntu-latest
    needs: [backend-targeted, frontend-build, frontend-targeted-e2e]
    permissions:
      contents: read
      statuses: write
    env:
      GH_TOKEN: ${{ github.token }}
      BACKEND_RESULT: ${{ needs.backend-targeted.result }}
      FRONTEND_BUILD_RESULT: ${{ needs.frontend-build.result }}
      FRONTEND_E2E_RESULT: ${{ needs.frontend-targeted-e2e.result }}
    steps:
      - name: Publish observable commit statuses
        shell: bash
        run: |
          publish_status() {
            result="$1"
            context="$2"
            description="$3"
            case "$result" in
              success) state=success ;;
              failure|cancelled) state=failure ;;
              *) state=error ;;
            esac
            gh api \
              --method POST \
              -H "Accept: application/vnd.github+json" \
              "/repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" \
              -f state="$state" \
              -f context="$context" \
              -f description="$description ($result)"
          }

          publish_status "$BACKEND_RESULT" "agent-ci/backend-targeted" "Targeted backend regression suite"
          publish_status "$FRONTEND_BUILD_RESULT" "agent-ci/frontend-build" "Frontend typecheck and production build"
          publish_status "$FRONTEND_E2E_RESULT" "agent-ci/frontend-e2e" "Targeted Playwright regressions"

          if [ "$BACKEND_RESULT" = success ] && [ "$FRONTEND_BUILD_RESULT" = success ] && [ "$FRONTEND_E2E_RESULT" = success ]; then
            overall=success
          else
            overall=failure
          fi
          gh api \
            --method POST \
            -H "Accept: application/vnd.github+json" \
            "/repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" \
            -f state="$overall" \
            -f context="agent-ci/overall" \
            -f description="Agent branch validation"
