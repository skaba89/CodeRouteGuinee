# Checklist rapprochement financier

- Verifier que seuls les roles `admin` et `super_admin` peuvent consulter les paiements.
- Controler les exports CSV financiers.
- Controler les logs d'audit `payments.reconciliation_items_viewed`.
- Suivre les paiements `failed` et `pending`.
- Comparer les montants par operateur.
